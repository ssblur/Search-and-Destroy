{
  "name": "search_and_destroy",
  "version": "1.0.0",
  "description": "A joke bot which plays a message when a user joins a channel on a supported server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrick Emery",
  "license": "ISC",
  "dependencies": {
    "@discordjs/opus": "^0.4.0",
    "discord.js": "^12.5.1",
    "ffmpeg": "0.0.4"
  }
}
